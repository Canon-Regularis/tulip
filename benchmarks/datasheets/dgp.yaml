# Datasheet prose for the dgp corpus ("Dialekty i gwary polskie" compendium).
# Renders via: tulip card datasheet <build-dir> \
#     --spec benchmarks/datasheets/dgp.yaml --dataset dgp
# Assembled manually and never redistributed with tulip; only aggregate digests and
# metrics are committed. Fields marked TODO are corpus-specific facts only you can
# supply; the rest are already filled.

motivation: >-
  TODO: state why this corpus was assembled. A typical answer: to complement
  dialektarium with a second, independently sourced real dialect corpus so
  cross-corpus transfer can be measured and a model cannot simply memorise one
  source's idiosyncrasies. Source: the DGP compendium at https://przewodnik.tmjp.pl/.

funding: "TODO: funding source(s), or 'none'."

collection_process: >-
  TODO: describe how the material was obtained from the DGP compendium and turned
  into text samples: which entries were used, who assembled and labelled them, and
  over what period. Note that this corpus is text-only unless you added audio.

sampling_strategy: >-
  TODO: describe how localities and samples were selected, the target count per
  dialect, and any geographic or demographic skew relative to dialektarium.

preprocessing: >-
  TODO: describe the cleaning you applied before writing the manifest. The benchmark
  configs add clean=true, deduplicate=true, and min_text_chars=20 at build time;
  note anything you did on top.

uses: >-
  Speaker-disjoint dialect identification benchmarking and cross-corpus transfer
  against dialektarium, plus demographic-bias slicing where age/gender metadata is
  present. TODO: note any out-of-scope or discouraged uses.

distribution_terms: >-
  The raw corpus is NOT redistributed with tulip. Only the split fingerprint,
  provenance, and aggregate metrics are committed, so the benchmark is reproducible
  in principle while the source text stays private. TODO: state the licence and terms
  under which you may hold and use the DGP material.

maintenance_contact: "TODO: name and contact for corrections, updates, and takedown requests."

ethical_legal: >-
  TODO: describe consent and anonymisation, and any community sensitivities around
  attributing text to a named dialect or locality. DGP is a published compendium;
  confirm your use of its material respects its terms.
