"""CLI command groups.

Each module here defines its commands against the shared Typer apps in
:mod:`tulip.cli._context`. Importing a module registers its commands. The
assembler in :mod:`tulip.cli.app` imports them all.
"""
